<view class="nav-bar" style="padding-top:{{statusH}}px;">
  <view class="back-btn" bindtap="goBack"><text class="back-icon">‹</text></view>
  <text class="nav-title">动态详情</text>
</view>
<scroll-view scroll-y class="body" wx:if="{{article.title}}">
  <text class="article-title">{{article.title}}</text>
  <view class="meta-row">
    <text class="meta-text">{{article.author}}</text>
    <text class="meta-text">{{article.date}}</text>
  </view>
  <view class="divider"></view>
  <text class="content">{{article.content}}</text>
</scroll-view>
<view class="loading" wx:if="{{loading}}"><text>加载中...</text></view>
